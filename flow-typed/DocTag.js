declare type DocTag = {
  tagName: string,
  tagType?: string,
  label?: string,
  isOptional?: boolean,
  defaultValue?: string,
  description?: string,
}

//endsWith(searchString: string, position?: number): boolean;
