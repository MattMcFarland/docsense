<code>
  {{#if type.type}}
    {{#eq type.type "AllLiteral"}}
      <i class="dark-red">any</i>
    {{/eq}}
    {{#eq type.type "NameExpression"}}
      <i class="dark-red">{{type.name}}</i>
    {{/eq}}
    {{#eq type.type "TypeApplication"}}
    <i>
      <b class="dark-red">
        {{type.expression.name}}
      &lt;</b>
        {{#each type.applications}}
          <span class="dark-red annotation" >
          {{#if @last}}
            {{name}}
          {{else}}
            {{name}},&nbsp;
          {{/if}}
          </span>
        {{/each}}
      <b class="dark-red">&gt;</b>
      </i>
    {{/eq}}
  {{else}}
  {{#if type}}
    <i class="dark-red">{{type}}</i>
  {{else}}
    <i class="dark-red">any</i>
  {{/if}}
{{/if}}
</code>
